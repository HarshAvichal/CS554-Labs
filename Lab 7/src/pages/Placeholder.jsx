export default function Placeholder({ title }) {
  return (
    <article className="page">
      <h1>{title}</h1>
      <p className="muted">This section is next on the build list.</p>
    </article>
  );
}
